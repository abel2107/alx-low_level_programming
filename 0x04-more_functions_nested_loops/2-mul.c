#include<stdio.h>
#include "main.h"
/**
 * mul - is used to multiplay the two numbers
 * @a: is used to accept the first number
 * @b: is used to accept the second number
 * mutiplay this numbers and give the result to res
 * Return: res
 */
int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
