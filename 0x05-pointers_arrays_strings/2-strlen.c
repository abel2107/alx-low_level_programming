#include<string.h>
#include "main.h"
/**
 * _strlen - will accept the string reference of a string and return the length
 * @s: is used to accept the string reference
 * Return: returns an int value of the string length
 */
int _strlen(char *s)
{
	int l = strlen(s);

	return (l);
}
